-- Minimal root: charged-lepton detuning ladder + obstruction (no SO(8) Lie closure / GeneratorsLieClosure).
-- `lake build` uses this as default target (see `lakefile.toml`).
import Hqiv.Physics.LeptonResonanceGlobalDetuning
